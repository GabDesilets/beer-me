{% extends '::base.html.twig' %}

{% block title %}
    Beer categories :: {{ parent() }}
{% endblock %}

{% block body %}
    <div class="box">
        <div class="box-header">
            <div class="row">
                <div class="col-sm-1">
                    <a href="{{ path('business.beer-category.new') }}" class="btn btn-primary btn-flat">
                        <i class="fa fa-plus"></i> Create a new category
                    </a>
                </div>
            </div>
        </div>
        <div class="box-body no-padding">
            <table class="table table-striped table-condensed table-hover dataTable">
                <thead>
                <tr>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for category in categories %}
                    <tr>
                        <td><a href="{{ path('business.beer-category.show', { 'id': category.id }) }}">{{ category.name }}</a></td>
                        <td>
                            <ul class="list-unstyled list-inline">
                                <li>
                                    <a href="{{ path('business.beer-category.show', { 'id': category.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('business.beer-category.edit', { 'id': category.id }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
